import { defaultColors } from '../../colors';

export const buttonWrapper = {
  display: 'flex',
  textAlign: 'end',
  justifyContent: 'center',
  alignItems: 'center',
};

export const listWrapper = {
  border: '1px #bcbebf solid',
  px: '1rem',
  mt: '0.5rem',
  borderRadius: '0.7rem',
};

export const iconStyle = { mr: '0.5rem', color: defaultColors.secondaryText };
